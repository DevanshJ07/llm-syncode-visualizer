fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
httpx==0.27.0

# ML / model inference
torch>=2.2.0
transformers>=4.36.0
accelerate>=0.26.0
sentencepiece>=0.1.99

# Syncode — grammar-constrained decoding (C / Python / Go / …)
# Builds a DFA mask store from the grammar on first load (cached to disk).
# Optional: the service degrades gracefully to raw mode if not installed.
syncode>=0.3.0

# Development / testing
pytest==8.2.0
pytest-asyncio==0.23.6
